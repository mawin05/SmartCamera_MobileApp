[project]
name = "camera"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dotenv>=0.9.9",
    "gpiozero>=2.0.1",
    "requests>=2.33.1",
    "rpi-lgpio>=0.6; platform_machine == 'aarch64'",
]
